// Shared with the original checkout design
export const inputClass =
    'block w-full rounded border border-surface-border bg-black p-3 text-white placeholder-text-muted/30 focus:border-primary focus:ring-1 focus:ring-primary sm:text-sm h-12 transition-all duration-300 aria-[invalid=true]:border-red-500/70';

export const labelClass = 'block text-xs uppercase tracking-wide text-text-muted font-medium';

export const optionCardClass =
    'group relative flex cursor-pointer rounded border border-surface-border bg-black p-5 shadow-sm transition-all hover:border-zinc-600 has-[:checked]:border-primary has-[:checked]:ring-1 has-[:checked]:ring-primary has-[:focus-visible]:ring-2 has-[:focus-visible]:ring-primary-bright';
